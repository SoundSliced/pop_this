name: pop_this
description: A powerful and customizable Flutter package for managing popups, toasts, and overlays with support for animations, stacking, and timers.
version: 4.0.0
homepage: https://github.com/SoundSliced/pop_this
repository: https://github.com/SoundSliced/pop_this
issue_tracker: https://github.com/SoundSliced/pop_this/issues

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  s_packages: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

